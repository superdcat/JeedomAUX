# Index mémoire

- [Vérif PHP sans `php -l` local](feedback_no_local_php_verification.md) — pas de binaire php dans ce shell (Windows/Git Bash) ; équilibrage `{}()[]` via script Python. Bug récurrent (×2, malgré mémoire déjà écrite) : AUCUNE commande Bash (`-c`, heredoc, heredoc quoté) n'est fiable pour transporter du texte `\t`/`\x00`/`\d` — toujours corrompu en octets bruts silencieusement ; utiliser l'outil `Write`/`Edit` (confirmé sûr) pour tout contenu contenant des séquences d'échappement, puis scanner les octets écrits avant de conclure. Sur fichier HTML+PHP mixte (`desktop/php/*`), ne stripper/compter QUE le texte entre `<?php ?>` — valider l'outil lui-même contre `git show HEAD:<path>`.
- [Edit tool échoue sur fichiers indentés en tabs](feedback_edit_tool_tab_indented_files.md) — `desktop/php/*.php` = tabs+CRLF ; recette : Write bloc à profondeur relative (vrais tabs) → sed prefixe plat pour re-baser → perl -0777 splice sur ancre littérale + normalisation CRLF ; si raté, `git checkout --` et recommencer plutôt que rafistoler.
- [Restriction permission configuration.php dépasse Read/Edit](feedback_configuration_php_permission_scope.md) — `diff a.txt b.php`/`md5sum`/`cat` bruts sur `configuration.php` refusés ; mais `git diff`/`git status --short` sur ce même chemin fonctionnent (confirmé) et sont la bonne façon de vérifier le sync `.txt→.php` après `cp`.
- [Périmètre d'un tour de correction post-review](feedback_coordinator_correction_round_scope.md) — implémenter strictement la liste numérotée du coordinateur `/feature`, jamais un détail additionnel déjà présent dans la spec technique qu'il amende en parallèle ; pour trancher un désaccord sur les fins de ligne, `tr -cd '\r'/'\n' | wc -c` (fiable) plutôt que `grep -c $'\r'` (dépendant du shell, a donné un faux positif chez le coordinateur).
